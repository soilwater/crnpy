# Documentation toolchain for `mkdocs build` / `mkdocs gh-deploy` (see mkdocs.yml).
# Versions verified with `mkdocs build --strict` on 2026-09-02. Material for MkDocs targets MkDocs 1.x;
# MkDocs 2.0 removes the plugin system, so MkDocs is pinned below 2.
mkdocs>=1.6.1,<2
mkdocs-material==9.7.7
mkdocs-material-extensions==1.3.1
mkdocs-autorefs==1.4.4
mkdocstrings==1.0.6
mkdocstrings-python==2.0.8
mkdocs-jupyter==0.26.3
mkdocs-glightbox==0.5.2
pymdown-extensions==11.0.2

# The 'social' plugin (social cards) additionally needs Pillow and CairoSVG, and on Windows the Cairo
# system library. Install with:  pip install "mkdocs-material[imaging]"
# If Cairo is not available, remove the '- social' line from mkdocs.yml before building.
